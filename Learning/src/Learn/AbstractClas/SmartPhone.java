package Learn.AbstractClas;

public interface SmartPhone {
	
	void calling();         // empty methods for the interface class
	void Browsing();		// All these methods are undefined so its called in a Interface Class
	void camera();			// No need to add Abstract keyword as all of them are undefined
	void computing();

}
